# Queue Linked List - Game Users (FIFO)

## Overview
This project implements a FIFO (First-In First-Out) Queue using a Linked List in C.

Each node in the queue stores a randomly generated game user with:
- Unique username (10 characters)
- Level (1–60)
- Faction (Red, Blue, Green)

The number of users is passed as a command line argument.

---

